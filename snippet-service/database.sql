CREATE TABLE snippets (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    favicon VARCHAR NOT NULL CHECK(LENGTH(favicon) > 0),
    title VARCHAR NOT NULL CHECK(LENGTH(title) > 0),
    text_preview VARCHAR NOT NULL CHECK(LENGTH(text_preview) > 0),
	link VARCHAR NOT NULL CHECK(LENGTH(link) > 0)
);