#For cassandra in cqlsh
CREATE KEYSPACE snippetsKeyspace WITH replication = {
    'class': 'SimpleStrategy',
    'replications_factor': 3
};

USE snippetsKeyspace;

CREATE TABLE snippets (
    id INT PRIMARY KEY,
    favicon VARCHAR,
    title VARCHAR,
    text_preview VARCHAR,
	link VARCHAR
);